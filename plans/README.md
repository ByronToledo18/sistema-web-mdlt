# Animation improvement plans — sistema-web-mdlt

Generated by `improve-animations` at commit `6501b96`, auditing everything **outside** what was already fixed in this same session via `animate` (button press feedback, cart drawer, cart item exit, AI design reveal, cart badge pulse, and the removal of the global unscoped hover rule).

## Plans

| # | Title | Severity | Status | File |
| --- | --- | --- | --- | --- |
| 001 | Add enter/exit transition to the checkout modal | HIGH | TODO | `001-checkout-modal-transition.md` |
| 002 | Transition on admin activo/inactivo status badges | MEDIUM | TODO | `002-admin-status-badge-transition.md` |
| 003 | Fix `.card-hover` transition-all and gate hover for touch | MEDIUM | TODO | `003-dashboard-card-hover-cleanup.md` |

## Recommended execution order

1. **001 (checkout modal)** first — highest severity, highest traffic (every purchase in the app goes through this modal), and fully independent of the other two.
2. **002 (admin status badges)** — independent of 001 and 003, touches multiple small admin files.
3. **003 (dashboard card-hover)** — independent, single-file CSS fix, safe to do any time, including in parallel with 001/002.

No plan depends on another; they can be executed in any order or in parallel by different agents.

## Not turned into a plan (noted, not forgotten)

- **LOW — `components/ui/switch.tsx:16`** — `transition-all` on the shared Switch primitive. Real impact is low (only `background-color`/`transform` actually change), so it didn't earn a full plan. One-line fix when someone's already in that file: change `transition-all` to `transition-[background-color,transform]`.
- **Missed opportunities** (additive, not corrective — no plan written, just flagged for whoever picks up delight/polish work next):
  - `app/portal/login/page.tsx`, `app/portal/registro/page.tsx` — error/success `Alert`s pop in with no transition.
  - `app/admin/pedidos/[id]/page.tsx` — newly added pedido items appear in the table with no highlight/confirmation.
  - `app/portal/pedidos/page.tsx` — order list cards could use a 30–80ms stagger on load (rarely-visited page, safe to add delight).

## Reconcile

Re-run `improve-animations reconcile` after any of these plans are executed to mark them DONE and refresh file:line references if the code has drifted.
